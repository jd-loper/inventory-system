# Inventory Management System API 📦

A robust RESTful backend application built with **Java Spring Boot** and **MySQL**. This API serves as a centralized system for tracking inventory, managing product details, and monitoring stock levels. It demonstrates enterprise-grade development patterns including layered architecture, input validation, and automated API documentation.

## 🚀 Key Features

* **CRUD Operations:** Full Create, Read, Update, and Delete functionality for inventory items.
* **Data Persistence:** Uses **MySQL** and **Spring Data JPA** (Hibernate) for reliable data storage.
* **Input Validation:** Prevents invalid data (e.g., negative prices, blank names) using **Jakarta Validation**.
* **Global Exception Handling:** Centralized error management to return clean, user-friendly JSON error messages.
* **Interactive Documentation:** Integrated **Swagger UI** for testing endpoints directly in the browser.

## 🛠️ Tech Stack

* **Language:** Java 25
* **Framework:** Spring Boot 3
* **Database:** MySQL Community Server
* **Documentation:** SpringDoc OpenAPI (Swagger UI)
* **Build Tool:** Maven
